<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="world.festival.dao.ListMapper">

<!-- 축제등록 테스트중 -->	
<insert id="writeFestival" parameterType="ListVO">
	INSERT INTO
		MAINBOARD (mainBoardNum,title,country,adress,festival_intro,inputTime,startEvent,endEvent,uploadFileName,saveFileName)
	VALUES (
		<!-- #{userid}, -->
		mainboard_seq.nextval,
		#{title},
		#{country},
		#{adress},
		#{festival_intro},
		sysdate,
		#{startEvent},
		#{endEvent}
		<choose>
			<when test="uploadFileName != null and saveFileName != null">
			,#{uploadFileName},#{saveFileName}
			</when>
			<otherwise>
			,null,null
			</otherwise>
		</choose>
	)
</insert>

<select id="printAll" parameterType="ListVO" resultType="ListVO">
	Select * from MAINBOARD

</select>

<select id="listDetail" parameterType="ListVO" resultType="ListVO">
	Select * from MAINBOARD where mainBoardNum = #{mainBoardNum}
</select>

</mapper>